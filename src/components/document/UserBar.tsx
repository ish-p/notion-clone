import { detailedDocsFormat } from "@/actions/document";

export default function UserBar({ data }: { data: detailedDocsFormat }) {
	return (
		<div>
			{/* Manage Users */}
			{/* Avatars */}
		</div>
	);
}
